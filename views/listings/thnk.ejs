<% layout("/layouts/boilerplate.ejs"); %>
<body>
  <div class="thankyou-container" style="text-align:center; margin-top: 100px;">
    <h2>Thank You, <%= name %>!</h2>
    <p>Your message has been received. We’ll get back to you shortly.</p>
    <p>Redirecting to homepage in <span id="countdown">3</span> seconds...</p>
  </div>

  <script>
    let countdown = 3;
    const countdownEl = document.getElementById("countdown");

    const timer = setInterval(() => {
      countdown--;
      countdownEl.textContent = countdown;

      if (countdown === 0) {
        clearInterval(timer);
        window.location.href = "/home";  // Adjust this if your homepage route is different
      }
    }, 1000);
  </script>
</body>
